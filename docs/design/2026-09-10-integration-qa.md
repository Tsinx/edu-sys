# 2026-09-10 课程与课堂修改整合验收

本次将工作区已有的港口动画、课堂全屏控制、Live2D 数字人、五层助教提示词管理，与新增第二讲逐字稿一起整合。

## 第二讲逐字稿

文件：`docs/course/港口管理概论-第二讲-90分钟逐字稿.md`。

- 52页逐页讲述，正文共16,270个汉字，操作提示另列；每页标题与课件登记一致。
- 时间逐页对齐 `PORT_LBL_LESSON_TWO.durationSeconds`，总计5400秒，第26页结束为2700秒。每分钟约152—213个正文汉字，时间安排包含观察、停顿及动画；尚未进行真实课堂试讲计时。
- 逐页人工对照场景与教学边界，明确单级船闸模型、上海水侧到达、假设中转分支、货物交付与空箱周转的差别。
- 计算复核：移动32天、准备作业3天、等待5天，合计40天；正常等待12小时、错班后等待166小时、额外等待154小时、出发延迟168小时。
- 相对文件链接有效；52页各有一组“操作”和“教师说”。本稿不进入学生画布。
- 本机核对结果：`output/lecture2-manuscript-audit.json`。该文件与其他本机输出按现有忽略规则不纳入 Git。

## 整合检查

- `pnpm test` 通过：仿真核心、平台API和教师前端测试，含学生文案、课程上下文、共享DOM、提示词作用域与持久化检查。
- `pnpm build` 通过：全工作区类型检查、KWS资源检查、生产构建。构建仍有大于500kB的分包提示。
- `pnpm avatar:live2d:verify`：51个文件校验和、25条模型资源引用通过。
- Git暂存区的51个Live2D资源也逐一核对哈希；该资源目录禁止换行转换，避免Windows重新检出后破坏原始文件校验和。
- `pnpm avatar:live2d:test`：13组浏览器检查通过，包括真实Core模型、音轨驱动口型、中断、取景与形象切换、窄屏、减少动态效果、模型故障与WebGL恢复。
- `pnpm --filter @edu/platform-api exec tsx ../../scripts/verify-assistant-prompts-browser.mts`：8组检查通过，五层修改进入文字与语音模型请求，桌面及390px布局通过。采用隔离API和捕获型模型提供器，不改变日常课堂提示词。
- 第2、3讲106页桌面/窄屏、75个工艺关键帧、5秒自动播放、全屏控制和PDF抽检已在同次工作中完成，详见[动画验收](./port-process-upgrade-qa.md)。其后仅新增逐字稿与文档，没有再次改动已验收的动画代码。
- `git diff --check`及暂存区检查通过。

以上不代表已验证实体教室音频、投影设备性能或真实模型回答质量。本次没有重打离线ZIP，也没有重新导出两讲全套PDF。
