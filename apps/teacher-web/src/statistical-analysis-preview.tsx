import {useEffect,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {STATISTICAL_ANALYSIS_SLIDES} from '@edu/course-content/statistical-analysis';
import {StatisticalAnalysisSlideStage} from './features/statistical-analysis/StatisticalAnalysisSlideStage';
function Preview(){const [index,setIndex]=useState(Math.max(1,Math.min(100,Number(new URLSearchParams(location.search).get('page'))||1)));const projection=new URLSearchParams(location.search).has('projection');const p=STATISTICAL_ANALYSIS_SLIDES[index-1]!;
useEffect(()=>{const url=new URL(location.href);url.searchParams.set('page',String(index));history.replaceState(null,'',url);const fn=(e:KeyboardEvent)=>{if((e.target as HTMLElement).closest('select,input,button'))return;if(['ArrowRight','PageDown'].includes(e.key))setIndex(i=>Math.min(100,i+1));if(['ArrowLeft','PageUp'].includes(e.key))setIndex(i=>Math.max(1,i-1));};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn);},[index]);
return <main className={`stats-preview ${projection?'stats-preview--projection':''}`}>{!projection&&<nav><button disabled={index===1} onClick={()=>setIndex(i=>i-1)}>上一页</button><select aria-label="课件页" value={index} onChange={e=>setIndex(Number(e.target.value))}>{STATISTICAL_ANALYSIS_SLIDES.map(s=><option key={s.index} value={s.index}>第{s.lesson}讲 · {s.localIndex} / {s.localTotal}　{s.title}</option>)}</select><button disabled={index===100} onClick={()=>setIndex(i=>i+1)}>下一页</button></nav>}<StatisticalAnalysisSlideStage frame={{index,versionId:'statistical-analysis-lbl-1',logicalWidth:1600,logicalHeight:1000,aspectRatio:'16:10',lessonNumber:p.lesson,lessonTitle:p.lessonTitle,section:p.section,deckId:'deck-statistical-analysis',slideId:p.slideKey,total:100,title:p.title,summary:p.summary}}/></main>;}
createRoot(document.getElementById('root')!).render(<Preview/>);
