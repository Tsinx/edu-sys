import { STATISTICAL_ANALYSIS_LESSONS, STATISTICAL_ANALYSIS_OBJECTIVES, getStatisticalAnalysisSlide } from '@edu/course-content/statistical-analysis';
import './course-overview.css';
export function StatisticalAnalysisCourseOverview({onStart,busy}:{onStart:(lesson:number)=>void;busy:boolean}) {return <section className="stats-course-overview" aria-label="统计分析方法课程目录"><p className="stats-course-intro">从研究问题与数据证据出发，衔接科研可视化、回归、问卷测量、因果推断与时间序列。32课时、16讲。前两讲各90分钟，由教师讲解与揭示证据。</p><h3>课程目标</h3><ol>{STATISTICAL_ANALYSIS_OBJECTIVES.map(o=><li key={o}>{o}</li>)}</ol><h3>讲次目录</h3><div className="stats-course-catalog">{STATISTICAL_ANALYSIS_LESSONS.map(l=><div key={l.number}><span>{String(l.number).padStart(2,'0')}</span><strong>{l.title}</strong>{l.status==='ready'?<button className="text-action" disabled={busy} onClick={()=>onStart(l.number)}>进入本讲 · {l.slideTotal}页</button>:<small>待建设</small>}</div>)}</div></section>;}
export function StatisticalAnalysisTeachingNotes({index}:{index:number}) {const p=getStatisticalAnalysisSlide(index);return <details className="stats-teacher-notes"><summary>教师讲解备注 · 第{p.lesson}讲第{p.localIndex}页</summary><p>{p.teachingCue}</p><small>本页建议约{p.minutes.toFixed(1)}分钟；停顿与翻页由教师决定。</small></details>;}
